import type { Metadata } from 'next'
import { pods } from '@/lib/data'
export const metadata: Metadata = { title: 'Agent Architecture' }
export default function AgentsPage(){return <><section className="page-hero-v2 engine-hero"><span>MULTI-AGENT ARCHITECTURE</span><h1>Structured disagreement is the feature.</h1><p>Behavioral, cultural, creative, and risk perspectives are separated on purpose so one confident generation pass does not masquerade as judgment.</p></section><section className="section"><div className="pod-grid expanded">{pods.map(p=><article key={p.index}><span>{p.index} / {p.signal}</span><h3>{p.name}</h3><p>{p.line}</p>{p.agents.map(([n,d])=><div className="agent-line" key={n}><b>{n}</b><p>{d}</p></div>)}</article>)}</div></section><section className="disclosure-band"><b>HUMAN GATE</b><p>Software roles in this prototype are evaluation lenses. They are not licensed legal, HR, compliance, financial, clinical, crisis, or other professional advice.</p></section></>}
