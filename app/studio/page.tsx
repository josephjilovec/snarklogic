import type { Metadata } from 'next'
import { PageHero } from '@/components/PageHero'
import { SectionHeading } from '@/components/SectionHeading'
export const metadata: Metadata = { title:'Performance Studio' }
const stages=[['WRITERS’ ROOM','Human creative leadership + agent-generated angle breadth.'],['PRE-VIS','Scripts, shot logic, boards, prompt systems, claims and supers mapped before production.'],['PRODUCTION ROUTING','Decide what should be human-shot, creator-led, generative, motion-designed, or hybrid.'],['QA + RIGHTS','Asset lineage, talent approvals, disclosures and policy checks are attached to the version.'],['MEDIA TEST','Creative enters a defined experiment matrix instead of disappearing into an ad account.'],['ITERATE','Performance findings become the next writers’ room constraints.']]
export default function Studio(){return <><PageHero kicker="ENTERPRISE PERFORMANCE STUDIO" title="A writers’ room wired directly into the experiment loop." copy="For brands that want the system plus hands-on execution: concepting, production architecture, creator coordination, versioning, risk review, and performance-informed iteration." cta="Bring us a campaign"/><section className="section-pad"><SectionHeading eyebrow="STUDIO PIPELINE" title="From funny idea to controlled deployment."/><div className="stage-list">{stages.map(([a,b],i)=><div key={a}><span>{String(i+1).padStart(2,'0')}</span><h3>{a}</h3><p>{b}</p></div>)}</div></section><section className="final-cta coral"><span>THE GOAL</span><h2>More shots on goal.<br/>Less creative amnesia.</h2></section></>}
